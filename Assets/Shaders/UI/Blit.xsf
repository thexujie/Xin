#include "Common.xsh"
#include "Quad.xsf"

cbuffer PixelShaderParameter
{
    float4 Color;
}

Texture2D Source;

float4 MainPS(float4 Position : SV_POSITION, float2 Texcoord : TEXCOORD) : SV_Target
{
    float4 Color = Source.SampleLevel(PointSampler, Texcoord, 0);
    return Color;

}
